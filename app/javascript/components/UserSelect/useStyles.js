import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  select: {
    paddingTop: 30,
  },
  label: {
    position: 'relative',
  },
}));

export default useStyles;
